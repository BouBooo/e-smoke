{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}

<div class="container mt-4">
    <a href="{{ path('admin_categories') }}" class="mb-4 btn btn-secondary">Retour</a>
{{ form_start(form) }}
    <div class="row">
        <div class="col">
        {{ form_row(form.value) }}
        </div>
        <div class="col">
        {{ form_row(form.liquids) }}
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Créer</button>
    
  {{ form_end(form)}}
</div>

{% endblock %}